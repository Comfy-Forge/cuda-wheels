# Overrides of the shared arch policy (defaults/arch_policy.yml)
# for this package only.
arch_list_by_cuda:
  # NATTEN's documented build floor is sm_80 (Ampere). Pre-Ampere arches
  # aren't listed as valid NATTEN_CUDA_ARCH values on natten.org/install,
  # even though the runtime check at backends/configs/checks.py is permissive
  # to sm_60. We track NATTEN's own supported matrix, not the runtime gate.
  #
  # Hopper (9.0) auto-enables -DNATTEN_WITH_HOPPER_FNA=1 in setup.py.
  # Blackwell DC (10.0/10.3) auto-enables -DNATTEN_WITH_BLACKWELL_FNA=1 and
  # requires CUDA 12.8+ — hence omitted from cu124/cu126 rows. Stripped from
  # Windows builds via patches/natten.py (MSVC can't parse the sm_100 FMHA
  # backward kernel templates).
  # Blackwell consumer (12.0, sm_120 / RTX 5090) doesn't trigger any
  # arch-specific NATTEN flags — it compiles via the regular CUTLASS-FNA
  # path and works on both Linux and Windows.
  '12.4': 8.0 8.6 8.9 9.0+PTX
  '12.6': 8.0 8.6 8.9 9.0+PTX
  '12.8': 8.0 8.6 8.9 9.0 10.0 12.0+PTX
  '12.9': 8.0 8.6 8.9 9.0 10.0 12.0+PTX
  '13.0': 8.0 8.6 8.9 9.0 10.0 10.3 12.0+PTX
  # 13.2 ADDED 2026-08-26 -- a copy of the 13.0 row, not a new policy. Without
  # it the CUDA 13.2 line fell through to arch_policy's row, which is
  #   7.5 8.0 8.6 9.0+PTX 10.0 12.0+PTX
  # and that is wrong three ways at once, on all 32 cu13.2 cells:
  #   * it ADDS 7.5, below NATTEN's documented sm_80 build floor -- forwarded
  #     verbatim into NATTEN_CUDA_ARCH, which has no allowlist;
  #   * it LOSES 8.9, so Ada gets no native cubin where 13.0 gives it one;
  #   * it LOSES 10.3, so B300/Blackwell-Ultra falls back to the sm_100 image.
  # A missing key is not "no override" -- resolve_arch_list() falls through to
  # the shared policy silently. Same shape as the sageattention ARM cu12.6 gap
  # found the same day. nvcc 13.2 supports sm_103, so 10.3 is valid here.
  '13.2': 8.0 8.6 8.9 9.0 10.0 10.3 12.0+PTX

# ---------------------------------------------------------------------------
# WINDOWS lane. Added 2026-08-26 together with the resolver's third lane
# (resolve_windows_arch_list). Until then Windows silently shared the x86
# fields above, so this divergence had nowhere to live but patches/natten.py,
# which stripped 10.0/10.3 out of NATTEN_CUDA_ARCH at build time while the
# resolver -- and anyone reading this file -- still said sm_100 was expected.
# C7 duly failed every natten Windows wheel:
#   [arch_sass] natten-...-win_amd64.whl: missing arch families sm_[10]
#     expected ['sm_100','sm_103','sm_120','sm_80','sm_86','sm_89','sm_90']
#     found    ['sm_120','sm_80','sm_86','sm_89','sm_90']
#
# The reason for the gap is genuine and not a build-tuning excuse: enabling
# Blackwell DC sets NATTEN_WITH_BLACKWELL_FNA, which pulls in
# csrc/.../sm100_fmha_bwd_kernel_tma_warpspecialized.hpp, and MSVC's strict
# mode rejects it with C2061 (syntax error, identifier). It is a compiler
# limitation on that header, not something a knob can move; there is no
# __CUDA_ARCH__ guard or shim that makes MSVC parse it.
#
# NOT a coverage decision taken lightly: sm_120 (RTX 5090 / Blackwell consumer)
# is unaffected and stays, because it compiles through the regular CUTLASS-FNA
# path and never triggers the Blackwell DC code. Only the DATACENTRE parts
# (B200/GB200 sm_100, B300 sm_103) are absent, and on Windows specifically --
# a combination that barely exists in the field. Linux and ARM keep both.
# Delete these rows the day MSVC or NATTEN can parse that header.
arch_list_by_cuda_windows:
  '12.4': 8.0 8.6 8.9 9.0+PTX
  '12.6': 8.0 8.6 8.9 9.0+PTX
  '12.8': 8.0 8.6 8.9 9.0 12.0+PTX
  '12.9': 8.0 8.6 8.9 9.0 12.0+PTX
  '13.0': 8.0 8.6 8.9 9.0 12.0+PTX
  '13.2': 8.0 8.6 8.9 9.0 12.0+PTX
