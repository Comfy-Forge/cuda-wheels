name: faithc_aot
links_torch: true   # compiled against libtorch: one wheel per (cuda x torch) pair
source_repo: PozzettiAndrea/faithc-aot
source_tag: v1.5.0-aot          # pinned tag on the fork (master @ 193bb9f)
clone_recursive: false
verify:
  # __init__ asserts CUDA availability at import ("AssertionError: CUDA is
  # not available.") -- not a torch device error, so the default patterns
  # miss it; the string form appends it as a forgivable pattern.
  expect_device_error: "CUDA is not available"
